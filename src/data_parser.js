//Initialise variables
let generic = [];
let product = [];
let category = [];
let addToCart = [];
let cart = [];
let estimate = [];
let conversion = [];

//Fetch csv

//Separate lines

//Filter lines and push to the right array
/*
    Tag générique

    Tag produit (prdr0 existe mais pas de prdr1, ref, estimate ou de scart)

    Tag catégorie (prdr1 existe mais pas de ref, estimate ou de scart)

    Tag ajout au panier (scumul : 1)

    Tag panier (scumul : 0)

    Tag vente (ref et amount)

    Tag devis (ref et estimate : 1)
*/

//Parse data in a table + add column input comment , 10 lines max
//Add 